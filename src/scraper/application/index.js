const FetchProducts = require('./FetchProducts');
const CreateProducts = require('./CreateProducts');

/** Con esto ya se exportan los modulos de ambos archivos */
module.exports = {
  FetchProducts,
  CreateProducts,
};
